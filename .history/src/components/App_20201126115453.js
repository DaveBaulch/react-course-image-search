import React from 'react';
import SearchBar from './SearchBar';

const App = () => {
  return (
    <di className="ui container" style="{{ marginTop: '10px '}}">
      <SearchBar />
    </di>
  );
};

export default App;
